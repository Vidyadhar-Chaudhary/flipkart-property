server:
  port: 8888

spring:
  application:
    name: configuration

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Vidyadhar-Chaudhary/flipkart-property.git
          default-label: main
          clone-on-start: true
#          search-paths: configs/properties